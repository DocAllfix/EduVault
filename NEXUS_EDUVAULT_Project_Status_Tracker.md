# NEXUS EDUVAULT — PROJECT STATUS TRACKER & MODEL STRATEGY

**Progetto:** Nexus EduVault v1.0  
**Cliente:** C.F.P. Montessori  
**Fornitore:** Axialoop di Di Lonardo Alessandro  
**Riferimento:** Execution Plan v4.0 + Blueprint v7.0  
**Ultimo aggiornamento:** 2026-05-31 (**F5 POST-MVP step 1/4 chiuso (backend + frontend Library tab). Plan post-MVP approvato + scritto in dettaglio in vast-hopping-sketch.md. F5 backend: `embeddings.py` (voyage-multimodal-3 image+text, 9 unit test PASS mock), `image_library_service.py` (k-NN cosine HNSW + GIN tag fallback, threshold 0.30), `diagram_router.py` (7 SVG heuristic confidence ≥0.5, 11 unit test PASS), migration 011_image_library_unique (idx supplementare). 2 endpoint REST: `GET /courses/{id}/image/library/search` rate 60/min + `GET /api/admin/images/library` paginato. Integration cascade tier-0 in `image_service._resolve_query_urls._one` PRIMA di Pexels (transparent: pool=None safe, threshold 0.30, usage_count++ best-effort). Frontend: `image-picker.tsx` riscritto con shadcn Tabs Library/Web default Library, LibraryCard grid 4-col hover license chip + score badge brand-primary + Tooltip attribution. mypy clean sui nuovi file, tsc -b strict + vite build CLEAN 7.11s. Sequenza skill REI-14 applicata. Discrepanze D-184/D-185/D-186 REI-16 (cascade ordering invertito, seed scope ridotto NO scraping auto, 8 SVG nuovi + ISO7010 posticipati). 20/20 unit test verdi totali F5. Resta F5.next: seed reale ~250 immagini + 8 SVG nuovi + ISO 7010 catalog. STORICO: F1 CATALOG ADMIN UI ✅ — MVP GATE CHIUSO. Backend `app/services/catalog_service.py` (CRUD + summary aggregate) + 6 endpoint admin REST gate VAA-c (approve/unapprove/bulk + filtri target/search/approved-only). Frontend `/admin/catalog` con tabella shadcn (3-card summary totali/approvati/pending + breakdown chip per target clickable filter + filtri ricerca + Switch only-approved + checkbox row con bulk approve + Dialog dettaglio moduli ordinati + bottoni Approva/Revoca per riga). CTA dalla pagina Admin. Flag `v2_catalog_from_db=false` default = safety-net D10: i 2 path config-driven e DB-driven coesistono finche' admin non approva tutti i 44 + flippa flag su Railway. D-183 REI-16 in VERIFICATION_DEBT (scope MVP ridotto: scraping script + research_agent branch posticipati a post-flip). mypy/tsc/vite build clean. Commit 86eeca8 deploy SUCCESS. **MVP CLIENTE USABILE**: rollback H8b + F4/F4b (badge + rigenera voce-aware) + F9 (regulation→courses discovery) + F3 D3 backend + F3.AI (3 LLM micro-actions per voce + free-text per modulo) + F1 (admin catalog review/approve). Prossimo: post-MVP F5/F6/F7/F8 quando deciso. STORICO: **). PRECEDENTE: 2026-05-31 (**F9 + F3.AI implementate e deployate. F9 (Regulation→Courses Discovery): backend `regulation_discovery_service.py` + endpoint `GET /regulations/{slug}/compatible-courses` + frontend collapsible ChunksSheet con badge coverage (generabile/corpus_thin/no_coverage) + bottone Genera per ogni corso compatibile. E2E Playwright VERIFIED prod: D.Lgs 81/08 → 6 corsi compatibili (3 generabile, 3 corpus_thin) tutti renderizzati con badge corretti e bottoni Genera. F3.AI (richiesta utente 2026-05-31 "anche solo cambiare il nome o l'ordine e passarlo in una chiamata"): rename "Approva scheletro"→"Approva struttura" + 3 azioni LLM per-voce (Riformula/Operativo/Alternative) + free-text "Chiedi all'AI" per-modulo. Backend `app/services/skeleton_ai_edit_service.py` (pure-proposal, no DB mutation) + 2 endpoint `POST /skeleton/ai-edit-{voice,module}` rate-limited 20/10 min con ownership+status gate. Frontend `skeleton-ai-edit.tsx` con DiffAcceptDialog (before line-through, after highlight brand-primary) + AlternativesDialog (3 card selezionabili) + ModuleAiPrompt (textarea validation 5-1000 char + preview dialog). Smoke API prod: action invalida → 400, action valida su corso completed → 409, endpoint montati e gate funzionante. Commit eba5415 + push, backend Railway deploy SUCCESS, Vercel deploy frontend SUCCESS. D-182 registrata in VERIFICATION_DEBT (plan §F3 dichiarava edit MANUALE, prompt utente prevale REI-16 — F3.AI NON è chat libera D7). MVP gate residuo: solo F1 (catalog UI admin). STORICO: **). PRECEDENTE: 2026-05-30 (**F2.13 B3 SQL pura IMPLEMENTATA — strada A completa (analista sign-off 2026-05-30): regulation_metadata.py con TOC verificata Normattiva (13 Titoli D.Lgs + 51 Allegati con I-bis/3A/3B/XLIII-bis/XLIII-ter post-D.L.19/2024 patente a punti) + Migration 008 add top_section column + indice idempotente + backfill prod via TCP proxy 1819 chunks D.Lgs (Titolo I=827, IV=419, VIII=118, IX=113, X=79, III=82, XI=39, V=27, II=25, VII=20, VI=15, XII=11, X-bis=9, XIII=5, Sconosciuto 30=1.6% parsing noise residuo) + 1154 single-section regulations (DM antincendio/Reg CE/DM 388) + apply_b3_cross_title_decay() con 5 casi (A keep_same_titolo, B keep_unclassified, C keep_no_dominante, D keep_skipped_insufficient_obs post-osservazione GEN M1 Art. 236 false-discard, decay×0.4 + soglia max_pool×0.30 + discard) + log strutturato 8 campi + flag b3_trivial_single_section/b3_noop_reason/b3_skipped_insufficient_obs + B3_MIN_OBSERVATIONS=4 settings (analista sign-off post-osservazione GEN M1: con n_obs<4 dominante è rumore statistico → skip B3 do-no-harm) + parser populate top_section in INSERT ingestion (cache _reg_slug + top_section_of(), zero backfill futuro). Test 6/6 verdi (B3) + 104/104 (regulation_metadata) + 5/5 (B2) + 2/2 (subtopic) = 117 totali. Mypy strict + ruff verdi sui file modificati. Mappa content_agent investigation: zero retrieve point post-materialize_module_from_skeleton (architettura confermata sana, content_agent riceve chunks_by_module pre-popolato dal pool B2+B3). Lezione bilaterale: criteri test "Art. 121/132/XLI decadenti" assumevano contaminazione al pool B2 voce-1, ma B2 cosine-diretto-al-subtopic-stretto fa pulizia su voce 1; contaminazione vista al PPTX entra al pool delle voci 2-N (retrieval_query progressivamente più larga). Mia campionatura strumentata sotto-stimata sistematica (1/9 voci per modulo). E2E ANT L1 post-B3 con flag ON è gate vero (H9). Resta: commit+push+Railway env V2_B3_CROSS_TITLE_DECAY_ENABLED=true + E2E ANT L1 strumentato (log tutte le voci di tutti i moduli + top-5 decadenti/scartati per voce con body[:120] + distribuzione b3_noop_reason aggregata) + PPTX 4 moduli all'analista per sample-read M0 + lettura combinata log. D-172 esplorativo: skeleton-tuning retrieval_query voci larghe se log mostrano voce sistematicamente contaminata anche post-B3.**). PRECEDENTE: 2026-05-30 (**B2 IMPLEMENTATA + E2E ANT L1 PASS sostanziale + analista H9 lezione estesa D-160 + B3 sign-off con 3 raffinamenti. B2 commit 7217737 (cosine_voyage selettore di pool top-K=30 fissa sul pool RRF top-100, Cohere downgrade a topical-affinity telemetry). E2E Antincendio L1 4h con flag V2_B2_COSINE_SELECTOR_ENABLED+SKELETON+RERANK tutti=true: 565s totali (-28% vs HACCP baseline), 335 slide 83+84+84+84 (ferita ANT V2-pre-D3 M3-76 risolta strutturalmente da D3), articoli del cuore ANT_M0 (Art. 40/46/Allegato I CPI) dominanti nei top-citati per modulo. ANALISTA SAMPLE-READ M0 84 slide (disciplina D-160 al render): ~28-30 on-topic veri + ~10-12 adjacent + ~35-40 cross-scope problematiche = ~42% problematico. Pattern: slide 31-39 meta-formazione corsi antincendio (9), slide 43-49 medico competente cross-modulo (5), slide 51-60 Titolo IV Cantieri cross-titolo (10), slide 61-70 ATEX Titolo XI parziali (5-8), slide 71-82 meta-formazione/ruoli formali (10). Differenza categorica ~33-42 punti percentuali invisibili alla mia regex strict (0.9% veri cross-corso). H9 NUOVA registrata: regex strict misura pattern noto del giorno, sample-read manuale è gate primario per refactor. Lezione D-160 estesa a metriche di evaluation produzione. B3 SIGN-OFF: (a) decay×0.4 + soglia scarto NON hard-scarto, (b) Titolo atteso del subtopic NON pool-dominante per evitare contaminazione REGIME 3 (fallback con cautela), (c) E2E ANT L1 post-B3 con sample-read analista come gate. Aspettativa post-B3 ricalibrata da "0.9%→0.0%" a "42%→15-20%": B3 elimina cross-titolo Cantieri/ATEX, lascia residue corpus-thin ANT target B4 + #R14-estesa riparsing DM 02/09. Resta: B3 implementazione + E2E + invio PPTX analista.**). PRECEDENTE: 2026-05-30 (**Ground-truth C' COMPLETO + analista sign-off B2/B3/B4. 5 moduli × 60 chunks = 296 classificati con motivazione_breve disciplinata + colonna pattern_misrank (classify cieca disconfermativa). 3 regimi reali emersi: REGIME 1 concept rich (GEN_M3, HACCP_M3) A1 utile 57%; REGIME 2 context rich (ANT_M0) A1 utile 74%; REGIME 3 corpus-thin per concetto (PRE_M3, GEN_M1) A1 utile 23%. Spearman intera 0.21-0.69 (sotto 0.7 target), GEN_M1 Sp top-30 NEGATIVO -0.089. Bottom-20 0 falsi negativi su tutti i 5 moduli. CORREZIONE METODOLOGICA analista: Spearman target sbagliato (assumeva ranker title-aligned forte); metrica corretta è ratio A1_utile/B_utile (≥2.3x su tutti i 5 moduli → cosine_voyage funziona come SELETTORE DI POOL su tutti i regimi). B2 architettura ridefinita: top-K cosine_voyage K=30 fissa, NON ranker fine. Ordinamento interno → B3 cleanup + cosine_voyage tie-breaker. B4 D9 vincolante: A1_utile<30% primario + Sp top-30<0.2 secondario diagnostico, alert+UI warning, NO blocco hard. Sequenza IMPLEMENTAZIONE incrementale: B2 K=30 fissa deploy + E2E delta vs baseline V2 → B3 deploy + E2E → B4 deploy → calibrazione finale B2 K adattiva. H6 chiusa (cosine_voyage selettore non ranker), H7 nuova (B2+B3 ordine in serie protezione drift), H8 nuova (REGIME 3 → scheletro doppio livello work-item esplorativo post-V2). D-172 riformulato (cross-encoder italiano-normativo post-V2 se necessario). Watchlist mis-rank universali catalogati (Allegato I esonero, Allegato IV schema, Allegato XIV cantieri). Resta: implementazione B2 K=30 fissa + E2E validation isolata, poi B3, poi B4. #R14 D.Lgs 193/2007 + DM 02/09 = completamento corpus per famiglia normativa, separato da calibrazione algoritmica.**). PRECEDENTE: 2026-05-30 (**D3 production-ready + 3 fix consecutivi post-E2E HACCP. (1) D-168 region filter EUROPEA passa come NAZIONALE — cosine 0→147 chunk per regulations UE (HACCP, CLP), commit 3759a73 + test 10/10 verdi. (2) D-169-bis audit 11 filtri silenziosi retrieval/recall — CHIUSO, zero altri filtri sospetti. (3) D-170 doppio LLM eliminato — refactor split `retrieve_for_subtopic` (D3, no autogen, DETERMINISTICO al 100%) vs `retrieve_for_module` (V2 legacy by-title, autogen). Two-run determinacy [PASS]: Δ top_score=0.0000, top-10 chunk_id list identica. Pattern generale registrato: prima di chiamare LLM, chiedersi se l'input è già nella forma giusta. Tests 2/2 verdi (autogen mock NOT awaited in D3 path, AWAITED in legacy). SCOPERTA 1 confermata: GEN M2 regime DENSO (top 0.988), patologia cross-titolo V2 era artefatto drop-list+expansions, NON corpus. REFACTOR_HYPOTHESES_CONFIRMED.md creato con H1/H2/H3 + difesa drift. Sign-off analista 2026-05-30: Bivio C, 5 moduli ground-truth (3 regimi reali: DENSO controllo + SPARSO calibrazione primaria + LCU stress B2↔B4). Tabella varianza eliminata 0.30→0.0000 conservata.**). PRECEDENTE: 2026-05-29 (**D3 SCHELETRO NARRATIVO completo end-to-end a livello codice — prima delle 4 cure post-review-17. STEP 1-7: modelli SkeletonItem/ModuleSkeleton + skeleton_service (grounding (a) pura, Azure mini) + pipeline interrupt_before + generation_service split research/content (resume via aupdate_state as_node, smoke LangGraph PASS) + API GET/PUT/POST skeleton + UI skeleton-review.tsx. mypy/ruff verdi, tsc EXIT 0. AUDIT analista GEN M1 PASS + STRESS-TEST PRE M3/ANT M0 PASS: scheletri puliti ZERO cross-corso anche sul caso peggiore V2 (PRE M3 era 90% cross-corso), top_score by-subtopic 0.64-0.99. Corpus antincendio ingerito 58→152 chunk (DM 03/09+01/09 vigenti). D-164 fix template quiz. Commit b16231c, ce4b0d8, 9551958, 6e61cca, 134add0, 77bc66b. Resta: E2E smoke completo flag-on + B2 (title-alignment) + B3 (KG sibling) + B4 (D9 sensori)**). PRECEDENTE: 2026-05-29 (**ANALISTA REVIEW 17 — V2 BOCCIATA, regress strutturale. Flag spenti, 3 V2 archiviati, F2.11 sospesa. Patologia: cross-corso contamination. 5 cure universali in sequenza**). graph_service.py 520 righe mypy+ruff verdi con `_ChunkResolver` cache + `executemany` batch (refactor critico: la versione N+1 droppava la connection TCP proxy Railway sul D.Lgs 81/08 da 1819 chunks). Backfill 7 normative in 78s: 21451 edge (18308 gerarchico_sibling + 2105 parent + 1003 cita + 26 attua + 9 modifica, tutti deterministic). 1-hop traversal in retrieval_v2: top rerank Art.15 (0.970) → top kg_1hop Art.6 (0.652) semanticamente valido. ingestion_service hook POST-embed dietro flag (default OFF) per regolarizzare l'edge extraction su ingestion future. Resta F2.9 (richiede flag-on temporaneo Railway + generazione 2 corsi paralleli ~30min). Cohere key su Railway, `app/services/retrieval_v2.py` (470 righe) mypy/ruff verdi, smoke test 4/4 PASS contro DB prod (top_score 0.97+, source=rerank_cohere), research_agent.py branch `if settings.v2_features.get("rerank_enabled")` con funzione separata `_retrieve_chunks_per_module_v2` testata standalone su 3 moduli reali (chunks #1 semanticamente perfetti). Flag default OFF → v1 invariata. Resta F2.5-F2.9: graph_service (edge deterministico+gerarchico+LLM gate Jaccard≥0.15), ingestion step POST-embed `extract_and_index_edges`, backfill 7 normative, 1-hop traversal, A/B prod. PRECEDENTE: 2026-05-28 (**v2 refactor FASE 0 — setup infrastrutturale**). Avviato il refactor v2 "universale" (piano `~/.claude/plans/vast-hopping-sketch.md`, approvato dall'analista col vincolo trasversale VAA — Anti-Allucinazione-silenziosa). FASE 0 completata: migrazione `app/db/migrations/005_v2_foundation.sql` applicata in prod via TCP proxy (7 tabelle nuove: course_type_catalog, course_type_modules, regulation_chunk_edges, image_library, conversations, messages, slide_quality_checks + 3 colonne `courses` per scheletro; 22 indici; idempotente; GRANT reso condizionale perché in prod il ruolo `nexus_app` NON esiste → l'app gira come `nexus_admin`, debito di sicurezza #R18 da chiudere pre-go-live). Feature flag system in `app/config.py` (10 flag `v2_*`, tutti default False eccetto `drop_list_enabled=True` safety-net → pipeline v1 INVARIATA, i 3 demo non cambiano). Deps `cohere 7.0.1` + `rank-bm25 0.2.2` installate (azure-speech rimandata a F7). `app/services/pipeline_telemetry.py` (helper emit/timed con `source` tracking, base monitoring D2 + sensori badge D9). mypy --strict + ruff verdi sui file F0. Ordine fasi v2: D8→D2+D1→D3→D9→D4/D5→D7→D6→D10. PRECEDENTE: 2026-05-27 (**Sessione #32 refinement Demo #2 v3 + velocità + frontend audio + chat fattibilità + chiusura gate analista**). Post review 12 analista (Demo #2 v2 M1 al 46% medico/biologico inaccettabile), implementati 4 fix chirurgici: (1) Query MODULE_QUERY_EXPANSIONS["Prevenzione e protezione"] ampliata 4→25 righe rimuovendo "sorveglianza sanitaria" che cosine-attraeva medico/biologico. (2) `_DROP_PATTERN_M1_PREVENZIONE_GENERALE` regex applicato SOLO al modulo "Prevenzione e protezione" GENERALE per filtrare medico/sorveglianza/biologico/cartella/vaccinazioni/cancerogeni. (3) Velocità: `_INSTRUCTOR_DEPTH_RETRIES` 5→2 (sub_batch recovery copre). (4) 4 nuovi test isolati verdi. **Frontend (su richiesta utente "abilita features cliente"):** AUDIO_POLL_TIMEOUT_MS 5min→12min (chiude #R-audio-fe-timeout-4h-only), AudioPlayer graceful onError ("Audio in elaborazione…"), ImagePicker empty state esplicito + smoke build pnpm verde 2.51s. **Doc fattibilità chat conversational** `docs/handoffs/CHAT_CONVERSATIONAL_FEASIBILITY.md` (7-10h work, FASE 8 post-deploy, riusa Vercel AI SDK + tool calling Claude). **Demo #2 v3 RISULTATI**: 9m 36s (-21% vs v2 12m 14s), 331 slide, M1 off-topic medico/biologico 46%→0% (azzerato), drop-list `chunks_dropped={1:9}` confermato live, contaminazione NON migrata (M0+2, M2+4, M3-1, netto -34 chunk eliminati). **ANALISTA REVIEW 13 VERDETTO**: "Demo #2 v3 passa il gate. Ship." + framing onesto Demo #3 v2 ("M3 Incidenti mancati ampio, near miss specifico richiede ingestione D.M./INAIL dedicata in versione produzione"). **3 demo APPROVATI**: Demo #1 E25 Specifica 4h (review 10), Demo #2 v3 Generale 4h (review 13), Demo #3 v2 Preposti 8h (review 12 + framing). Stato: **FASE 7 deploy in corso** (commit + push branch + Railway pgvector custom + Vercel + caricamento 3 PPTX storage + DevTools test + invio URL analista per sanity check + email cliente). **60/60 test #31.x+#32 verdi**, 116 pytest obsoleti pre-esistenti (debt #30.x da risincronizzare post-demo). **D-142/D-143/D-144** nuove discrepanze meta-architetturali + **#R-chat-conversational** + **#R-audio-fe-timeout-4h-only CHIUSA**. PRECEDENTE: 2026-05-27 (**Sessione scaling pipeline #31.1→#31.8** — preparazione 3 demo cliente pre-deploy). 11 review analista in 2 giorni, ~14h work cumulato, focus su scaling pipeline + qualità diagrammi. **STATUS attuale FASE 7 deploy preview:** Fix #31.5/6/7/8 pronti (commit pending) + Demo #1 Specifica 4h pronto sul Desktop + Demo #2 Generale 4h v2 in rigenerazione live con #31.8 (task `bchoat9q1`, telemetry conferma leve A+B+C OPERATIVE: `top_k=67`, `pinned_count=120` su 4 moduli, M3 da 70 a 40 chunk = trasferimento Segnaletica a M2 funziona) + Demo #3 Preposti 8h v2 in coda dopo Demo #2 (REI-3 Semaphore(1)). **Pre-cleanup eseguito**: 21 docs handoff/analyst spostati in `docs/handoffs/`, `.gitignore` esteso (frontend/dist, storage/output, .claude lock), `from typing import Any` aggiunto in `ingestion_service.py` (fix ruff F821), `json` unused rimosso da `rebuild_service.py`. **VERIFICATION_DEBT aggiornato**: D-137..D-141 (5 nuove discrepanze #31.7A+#31.8), #R16/#R17 (DM 02/09/2021 + Reg CE 852/2004 mancanti per Antincendio/HACCP — work-item post-demo), #R-pgvector-railway + #R-cors-explicit-vercel (deploy preqs analista review deploy). **Sequenza FASE 7 in attesa decisione utente** post-Demo #2+#3 v2: cleanup → commit atomici #31.5/6/7/8 → push branch `feat/phase6-frontend-shadcn` → draft PR → DevTools Flow A LOCAL → presentazione utente per OK deploy → Railway (pgvector custom + backend + ingestion) + Vercel (vercel.json + dist frontend). **60 nuovi test #31.x verdi** (8 #31.8 + 16 #31.7A + 6 #31.5B + 5 sub_batch + 15 #31.6B + 4 #31.6D + 6 #31.1), 116 pytest obsoleti pre-esistenti (pacing_engine + slide_constraints da iterazioni #30.x) NON regressioni. PRECEDENTE 2026-05-26 (**Sessione demo qualità post-FASE-6** — corsi reali end-to-end). Risolti 2 blocchi qualità emersi nei test live: **(1) Normative mancanti**: i corsi lavoratori crashavano perché lo slug `accordo_stato_regioni_2011` del COURSE_CATALOG non era mai stato ingerito. Scaricati da fonti ufficiali (gazzettaufficiale.it / lavoro.gov.it) + ingeriti via nuovo `scripts/ingest_accordi.py`: Accordo 2011 (27 chunk), 2025 (133 chunk), 2016 (degradato — PDF scansionato, #R14). Catalog corsi lavoratori/preposti aggiornato all'**Accordo 17/04/2025 vigente** (il vecchio 2011 è scaduto 23/05/2026, il cliente non può più erogarlo). **(2) FIX #24 RECAP** validato su corso #14: slide RECAP con 5 bullet `nx_recap_text_710-750` tutte popolate + checkmark ✓ (era body vuoto + titolo sovrapposto nel corso #11). **(3) FIX #25 zero-placeholder**: `image_service.prefetch_images` ora fa backfill throttled dei buchi immagine (1 wave + pausa 20s, cap 150s) + fallback brandizzato C.F.P. (gradiente rosa + pittogramma tematico DejaVuSans + barra verde con caption) per i residui; caption `nx_caption` pulita (no più sintassi `[ query ]`). Verificato: `prefetch_images` porta i buchi a 0 su slide reali (provider + fallback). Corsi demo generati: #14 Primo Soccorso 8h (883 sl), #15 Generale 4h (433 sl), #16/#17/#18 Specifica Basso 4h. Nuove discrepanze D128-D136, risorse #R14/#R15 in VERIFICATION_DEBT.md; #R9 parzialmente chiuso. PRECEDENTE 2026-05-24: (**FASE 6 COMPLETATA ✅** — 10/10 prompt + audit checklist 20-item. Frontend production-ready: 17/17 endpoint BP §10 cablati, WS+polling 30s, JWT auth guard, 7 pagine operative (Login, Dashboard, Wizard 6-step, Progress Monitor, Course Detail, Regulations, Admin), sidebar role-aware, dark mode, branding C.F.P. Montessori applicato (OKLCH brand tokens in theme.css + 10 varianti logo auto-gen Pillow). **`pnpm/vite build` verde 3.37s, 0 errori, 0 warning, 56 chunks**. **Smoke E2E LIVE eseguito** (Chrome DevTools MCP): logout→login→dashboard→wizard→POST `/api/courses` 200 (estimated_slides=480 == backend PacingEngine)→progress page render con phase machine corretta. Audit 20/20: 18 ✅ + 2 ⚠️ parziali (item 15 gestione utenti = stub onesto perché `/api/users` non esiste backend REI-5; item 20 Download blocked-upstream da #R3 Anthropic key + #R11 LangGraph checkpoint setup — frontend pronto fino a Progress). 60 nuove discrepanze REI-16 (D68-D127) in VERIFICATION_DEBT.md (117 totali). Per chiusura definitiva 2 item parziali e flusso E2E completo download: serve #R1 (PDF DM 388) + #R3 (Anthropic) + #R4 (Voyage) + #R11 (langgraph setup) + endpoint backend `/api/users` (FASE 7).)  

---

## LEGENDA

| Simbolo | Significato |
|---|---|
| ⬜ | Non iniziata |
| 🔄 | In corso |
| ✅ | Completata |
| ❌ | Bloccata / Fallita |
| 🟡 | Completata con riserva (da rivedere) |

### Modelli Claude Code

| Sigla | Modello | Costo (per 1M token) | Quando usarlo |
|---|---|---|---|
| **S** | Sonnet 4.6 | $3 input / $15 output | Default. Task con specifiche chiare, codice da blueprint, CRUD, config, test |
| **O** | Opus 4.7 | $5 input / $25 output | Task complessi: algoritmi custom, architettura multi-file, prompt engineering, UI articolate |

> **Regola d'oro:** Opus costa ~1.67× Sonnet per token, ma usa più thinking token. In pratica su task complessi **Opus costa 2-3× Sonnet per sessione**. Usarlo solo dove il ragionamento profondo evita rework che costerebbe di più in sessioni Sonnet ripetute.

---

## STEP PRE-SVILUPPO

### STEP A — Inizializzazione CLAUDE.md + Struttura Cartelle

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| A.1 | Creazione CLAUDE.md | **S** | Copia esatta da spec, zero ragionamento | ✅ | 2026-05-23 | CLAUDE.md scritto + REI-12/13 aggiunte, .env.example creato, hook SessionStart onboarding configurato. |
| A.2 | Struttura cartelle backend | **S** | Creazione file vuoti da lista, meccanico | ✅ | 2026-05-23 | Struttura BP §14.1 creata in root EduVault: 25 dir, 51 file vuoti, 12 .gitkeep, README, .gitignore. Frontend rimandato a FASE 6. |

### STEP B — Ricerca Estensioni / Skills / MCP

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| B.1 | Analisi ambiente locale | **S** | Comandi shell + report tabellare | ✅ | 2026-05-23 | ENV_REPORT.md generato. Tutto conforme BP §1.1, solo psql client mancante (non bloccante). |
| B.2 | Ricerca MCP/Skills/Estensioni | **S** | Web search + filtro, no ragionamento architetturale | ✅ | 2026-05-23 | docs/TOOLBELT.md v1 → v2 (con frontend-design, langchain-skills, Impeccable, UI/UX Pro Max, shadcn skill, Vercel guidelines; styled-components scartato con motivo). |
| B.3 | Installazione selettiva | **S** | Esecuzione comandi uno per uno | ✅ | 2026-05-23 | **21 skill + 1 slash command + 6 MCP + 12 estensioni VS Code installati.** Skill: 7 design (impeccable + ckm:*) + karpathy-guidelines + frontend-design (Anthropic) + 6 langchain/langgraph + 6 chrome-devtools (cdt-*). Slash command: /code-review (Anthropic ufficiale). MCP: github, shadcn, playwright, postgres-restricted, codegraph, chrome-devtools. Plugin Anthropic ufficiali estratti manualmente dai loro repo sorgenti perché /plugin non funziona nell'estensione VS Code (bug noto #8569/#8590/#58556). Aggiunto REI-15 (auto-reindex codegraph). superpowers@obra rimandato a post-FASE 1. |

### STEP C — Questionario Cliente

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| C.1 | Stesura questionario | **S** | Template strutturato da spec | ✅ | 2026-05-23 | docs/CLIENT_INTAKE_QUESTIONNAIRE.md — 8 sezioni in italiano, ancorate a BP §00/§06/§13 (6 tipi corso + HACCP regionale Campania) + REI-13 (dominio TBD), 4 sezioni bloccanti go-live esplicitate. |
| C.2 | Tracking ricezione materiali | **S** | Tabella Markdown semplice | ✅ | 2026-05-23 | docs/CLIENT_INTAKE_TRACKING.md — 27 item allineati al questionario (1.1→8.3 + voce semantica "5 VPS"), 4 bloccanti go-live evidenziati, path convention da BP §14.1, stati ⏳/🔄/✅/❌/⚪. |

---

## FASI DI SVILUPPO

### FASE 0 — Infrastruttura (Sprint 0)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 0.1 | `pyproject.toml` | **S** | Copia tabella dipendenze da BP §1.1, meccanico | ✅ | 2026-05-23 | 22 runtime deps (BP §1.1 verbatim) + 4 OPT v3.0 (edge-tts/mutagen/pydantic-settings/Jinja2); openai assente. |
| 0.2 | `Dockerfile` | **S** | Copia da BP §02.1 con adattamento minimo | ✅ | 2026-05-23 | BP letterale + pin a bookworm (libgdk-pixbuf2.0-dev trixie-removed) + COPY app/ pre pip install (hatchling). |
| 0.3 | `docker-compose.yml` | **S** | Copia da BP §02.2, 4 servizi | ✅ | 2026-05-23 | 4 servizi BP §02.2 + profile "full" su frontend/nginx (PHASE 6+). postgres expose-only. |
| 0.4 | `main.py` + `config.py` + `dependencies.py` + `connection.py` | **S** | BP fornisce codice esatto, pydantic-settings è boilerplate | ✅ | 2026-05-23 | OPT-2 (Settings) ovunque, zero os.environ[]. Lazy import generation_service (FASE 5). |
| 0.5 | Endpoint `/health` | **S** | Endpoint banale, 20 righe | ✅ | 2026-05-23 | BP §10.1 via APIRouter, get_pool() centralizzato, fallback disk path host-friendly. 4 test integration verdi. |
| 0.6 | Verifica E2E Sprint 0 | **S** | Comandi docker + curl | ✅ | 2026-05-23 | docker compose build+up OK; /health → {"status":"ok","database":"connected","disk_free_gb":948.9} HTTP 200. Tag v0.1.0-phase0. |

**Stima costo FASE 0:** ~$2-4 (100% Sonnet)

---

### FASE 1 — Database, Auth, Modelli Pydantic (Sprint 1)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 1.1 | Schema SQL `001_initial.sql` | **S** | Copia da BP §03, SQL dichiarativo | ✅ | 2026-05-23 | BP §03 letterale + GAP-3 (audio_tracks dopo image_cache; courses.audio_manifest_path). 10 tabelle, HNSW, GIN. Applicato live: zero errori. |
| 1.2 | Modelli Pydantic (4 file) | **S** | Copia da BP §04, Pydantic boilerplate con validator | ✅ | 2026-05-23 | BP §04 letterale + outputs=audio whitelist + fix lookup enum nel body validator. 19 unit test verdi. |
| 1.3 | Servizio Auth (JWT + bcrypt) | **S** | BP §08 fornisce codice esatto, logica lineare | ✅ | 2026-05-23 | BP §08 + OPT-2 (settings) + karpathy-guidelines (assumptions A1-A14 dichiarate). pyjwt aggiunta. Limiter shared in api/dependencies. 15 integration test verdi (login, refresh, /me, type-check, is_active, no-enum). |
| 1.4 | Seed admin | **S** | Script semplice, idempotente | ✅ | 2026-05-23 | BP §02.7 + OPT-2 + structlog. Refactored seed(pool) per testabilità. 6 unit test (incl. idempotenza split admin/brand). |
| 1.5 | Smoke test FASE 1 | **S** | Comandi sequenziali di verifica | ✅ | 2026-05-23 | E2E live: down -v → up → migrations (10 tables) → setup_roles (nexus_app + REVOKE) → seed + idempotenza → login → /me (admin, is_active) → DELETE/UPDATE/TRUNCATE audit_log come nexus_app TUTTI permission_denied. .env rigenerato con hex (no padding =). codegraph reindex eseguito (115 file, 1415 nodi, REI-15). |

**Stima costo FASE 1:** ~$3-5 (100% Sonnet)

---

### FASE 2 — Knowledge Base + COURSE_CATALOG + RAG (Sprint 2)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 2.1 | Estrazione e Parsing PDF | **S** | pdfplumber wrapper semplice | ✅ | 2026-05-23 | BP §06.1.1 Stadio 1 letterale + accetta str/PathLike + FileNotFoundError esplicito. 5 integration test verdi (real PDF preferred, synthetic fixture fallback). 49/49 test totali, mypy/ruff verdi. Codegraph reindex eseguito (REI-15 trigger d). **NOTA:** PDF reale dm388_03.pdf ancora mancante in storage/pdfs/ — fixture sintetica reportlab in tests/fixtures/pdfs/dm388_synthetic.pdf (4 pagine, Art. 1/2/2-bis/Allegato I). 2.2 chunking richiederà il PDF reale per validare coverage regex su testo gazzettiero. |
| 2.2 | Chunking Ibrido | **O** | ⚠️ Logica custom complessa: regex normative italiane (ART_PATTERN, COMMA_PATTERN, ALLEGATO_PATTERN), coverage check, fallback, dedup. Il cuore differenziante del prodotto. Errori qui si propagano a cascata. | ✅ | 2026-05-23 | BP §06.1.1 Stadio 2 letterale inline in ingestion_service.py (no utils/ separato). 8 entità: ART_PATTERN (-bis..-decies), COMMA_PATTERN, ALLEGATO_PATTERN, normalize_for_coverage, chunk_structured/unstructured_regulation, extract_uncaptured_text, chunk_regulation (coverage 70%) + compute_content_hash SHA-256. Alias Chunk=dict[str,str\|None] (no TypedDict per CLAUDE.md). 18 test (15 unit regex/coverage/hash + 3 E2E PDF). 67/67 totali, mypy/ruff verdi. karpathy applicato. **NOTA: testato SOLO su fixture sintetica — coverage regex su PDF gazzettiero reale ancora da validare (PDF mancante).** |
| 2.3 | Classificazione + Embedding | **S** | API call + batch processing, logica lineare | ✅ | 2026-05-23 | BP §06.1.1 Stadio 3+4 inline. classify_chunk (LLM + downgrade SANZIONE rule-based), embed_batch/voyage_embed_with_retry (Voyage voyage-3, get_voyage_client da dependencies), index_chunks (batch 50 + dedup content_hash). **GAP sequenza:** call_llm (BP §05.5, normalmente in content_agent FASE 3.4) definito QUI con anthropic+tenacity per sbloccare 2.3 — FASE 3.4 lo importerà da ingestion_service (deciso con utente). OPT-2: timeout da settings.llm_request_timeout. 8 test mock Voyage/Anthropic. 75/75 totali, mypy/ruff verdi. dependencies.py invariato (set/get_voyage_client già FASE 0). |
| 2.4 | COURSE_CATALOG | **S** | Dizionario Python statico da BP §13 | ✅ | 2026-05-23 | config/catalog_config.py: 6 tipi corso verbatim BP §13 (sicurezza generale/specifica basso, primo soccorso B/C, antincendio L1, HACCP addetto, preposti). HACCP regional=True (unico regionale v1.0). Type hint dict[str,dict[str,object]] (no TypedDict). Import `from config.catalog_config import COURSE_CATALOG` (namespace package, no __init__). 8 test (6 tipi, titoli/regs/moduli non vuoti, HACCP regionale, bounds ore). 83/83 totali, mypy/ruff verdi. |
| 2.5 | KnowledgeRepository + RAG | **O** | ⚠️ Query vettoriale con JOIN regionale NULL-safe, filtro rilevanza, pattern complesso. Errori producono corsi con contenuto errato. | ✅ | 2026-05-23 | services/knowledge_repo.py (NON rag_service.py) BP §06.3: resolve_slugs_to_ids (ValueError su slug mancanti), search_chunks (JOIN regulations.region NULL-safe, relevance_score = 1 - cosine, ORDER BY <=>, LIMIT top_k), get_style_patterns (ORDER BY certified_at DESC LIMIT 5). Helper _to_pgvector: embedding list→literal '[...]' perché asyncpg non ha codec vector (segnalato REI-16). langchain-rag skill applicata (filtro ibrido vector+scalar su pgvector, NON retriever astratto; embedding voyage-3/1024 coerente index↔query). 9 test su mock pool (gotcha #4 — DB live in E2E). 92/92 totali, mypy/ruff verdi. |
| 2.6 | Endpoint REST `/api/regulations` | **S** | CRUD standard con rate limit | ✅ | 2026-05-23 | api/routes/regulations.py BP §10: POST /upload (require_role admin + rate 3/min §08.5 + UploadFile/Form → ingest_regulation_file), GET / (paginato page/per_page), GET /{id}/chunks (paginato), DELETE /{id} (soft-delete UPDATE status='ABROGATA', 404 se assente). Router registrato in main.py. ingest_regulation_file (orchestratore Stage 1-4, scope deciso con utente) + fix embedding→$N::vector literal in index_chunks. 8 test (authz operator→403, paginazione LIMIT/OFFSET, soft-delete, upload mock). 100/100 totali, mypy/ruff verdi. |

**Stima costo FASE 2:** ~$8-12 (4 Sonnet + 2 Opus)

---

### FASE 3 — Agenti LangGraph + PacingEngine (Sprint 3)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 3.1 | State LangGraph + Grafo (2 nodi) | **S** | BP §05.2-§05.3 fornisce codice esatto, pattern LangGraph standard | ✅ | 2026-05-23 | app/agents/pipeline.py: NexusPipelineState TypedDict (8 campi BP §05.2 letterali, no pptx/pdf — FIX-1) con operator.add reducer su completed_modules+errors. create_pipeline @asynccontextmanager (D18: AsyncPostgresSaver.from_conn_string è acm in langgraph-checkpoint-postgres 3.1.0). Grafo: research+content via set_entry_point/add_edge/set_finish_point BP §05.3 letterale. 2 NODI, no Production Builder. Stub research_agent/content_agent (impl 3.3/3.4). Lazy import per evitare ciclo (D20). pyproject + langgraph-checkpoint-postgres + psycopg[binary] (D21). 7 test compile (TypedDict shape, no campi vietati, reducer, 2 nodi, edge lineari, checkpointer wired, acm signature) — mock InMemorySaver, Postgres MAI live (#R2). 108/108 totali, mypy/ruff verdi. langgraph-fundamentals + langgraph-persistence skill applicate (REI-14). |
| 3.2 | PacingEngine (30s/slide) | **S** | Calcolo matematico deterministico, formula chiara | ✅ | 2026-05-23 | app/services/pacing_engine.py (NON agents/) BP §06B + GAP-1+FIX-8 v2.0. SECONDS_PER_SLIDE=30 fisso (D22 sostituisce media ponderata BP), DISTRIBUTION 5 tipi senza DIAGRAM (D23 FIX-8), DENSITY_MULTIPLIER 0.8/1.0/1.25, SLIDES_PER_MODULE_TARGET=40. Per-modulo: l'ultimo tipo assorbe il resto (somma=slide_count). module_titles dal COURSE_CATALOG con fallback "Modulo N". 17 test deterministici PURI (zero mock, zero I/O): 1h→120, 4h→480, 8h→960, 16h→1920 slide; densità leggera 4h→384, intensiva→600; DIAGRAM assente dovunque; distribuzione somma a slide_count per ogni modulo; min 2 moduli; titoli catalog vs fallback. 125/125 totali, mypy/ruff verdi. **PacingEngine è uno dei rari componenti SENZA verification debt** (#R0 non applicabile). |
| 3.3 | Research Agent | **O** | ⚠️ Orchestrazione complessa: query semantica, top_k dinamico, gate RAG, distribuzione chunk con keyword overlap, ribilanciamento min/max. Molte edge case. | ✅ | 2026-05-23 | app/agents/research_agent.py BP §05.4 letterale. Helpers: _keyword_overlap, _rebalance_min/max, distribute_chunks_to_modules (round-robin se <3*moduli chunks, altrimenti semantic overlap + rebalance min(3)/max(avg+5)). Pipeline: resolve_slugs→regional guard (HACCP+NAZIONALE→ValueError, IMPLEMENTA item 7 checklist FASE 2 che D17 segnalava appartenere a FASE 3 → DEBITO CHIUSO)→query semantica D-20 (title+default_modules)→voyage_embed→search_chunks(top_k=max(30, durata*10))→gate <5→filtro MIN_RELEVANCE 0.3→pacing con titoli catalog→distribute→get_style_patterns. Return dict {course_context, pacing_plan} (langgraph fix-state-must-return-dict). 13 test (7 helpers det. + 6 mock flow). 138/138 totali, mypy/ruff verdi. D24: aggiunto config/__init__.py per mypy. langgraph-fundamentals + langchain-rag skill (REI-14). |
| 3.4 | Content Agent + Circuit Breaker | **O** | ⚠️ La sotto-fase più complessa del progetto: chiamate LLM con retry, parsing JSON robusto, circuit breaker inline, prompt engineering differenziato Discente/Formatore. Errori = corsi illeggibili. | ✅ | 2026-05-23 | app/agents/prompts.py BP §05.6 letterale (SYSTEM_PROMPT_DISCENTE/FORMATORE + build_content_system_prompt + build_module_prompt diff. formatore + build_previous_summary). app/agents/content_agent.py BP §05.5 letterale: parse_slides_json (fence strip + json.loads + retry correttivo), content_agent iterates pacing.modules[start_index:], call_llm via retry tenacity, circuit breaker INLINE failed_count: int (FIX-3 — NO class). D10 CHIUSO: import call_llm da ingestion_service (no duplicazione). Reducer-friendly return {completed_modules:[...], current_module_index:N} (langgraph fix-state-must-return-dict). 14 test: parser+prompts deterministici, happy path 3 moduli, corrective retry, breaker trip>50%/no-trip=50%, meta-test grep FIX-3. 152/152 totali, mypy/ruff verdi. karpathy-guidelines + langgraph-fundamentals skill applicate (REI-14). |
| 3.5 | Pipeline E2E (senza build) | **S** | Test di integrazione, non logica nuova | ✅ | 2026-05-23 | tests/integration/test_pipeline_e2e_no_build.py. **Approccio mock+live deciso con utente** (D25): test PRIMARIO mockato (6 dipendenze patched simultanee: AsyncPostgresSaver→InMemorySaver, KnowledgeRepository.resolve/search/style, voyage_embed, call_llm) — verifica ainvoke 2-nodi, reducer operator.add accumula completed_modules, checkpoint scritto (graph.aget_state), asyncio.wait_for(timeout). Test SECONDARIO timeout: nodo hang 5s vs timeout 0.1s → asyncio.TimeoutError. Test LIVE skeleton @pytest.mark.live (skip default, eseguibile `pytest -m live` quando #R1+#R2+#R3+#R4 disponibili). 154/154 mocked verdi + 1 deselected. mypy/ruff verdi. codegraph reindex. |

**Stima costo FASE 3:** ~$10-15 (3 Sonnet + 2 Opus)

---

### FASE 4 — Production Builder (Sprint 4)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 4.1 | `inspect_pptx_template.py` | **S** | Script CLI di ispezione, output report | ✅ | 2026-05-23 | scripts/inspect_pptx_template.py: argparse `--template-path` / `--output-json`, dataclass-driven (ShapeInfo/LayoutInfo/InspectionReport), enumera slide_layouts ed estrae idx/name/shape_type/placeholder_type/posizione in EMU+inches. Output: report tabellare stdout + JSON in `assets/templates/master_inspection.json`. Exit 1 con messaggio chiaro se template assente (LAVORO UMANO #R8). Smoke test su `.pptx` default OK (11 layout/58 shape rilevati, JSON valido). D28 (struttura inspector dedotta dal contesto BP §07.3). #R10 chiuso: deps FASE 4 installate localmente. `pptx`/`pptx.*` aggiunti a mypy overrides. mypy/ruff verdi, 154/154 verdi. |
| 4.2 | SlideBuilder | **O** | ⚠️ Manipolazione python-pptx con layout complessi, posizionamento pixel, inserimento immagini con fallback, gestione template multi-layout. Richiede comprensione visiva della struttura PPTX. | ✅ | 2026-05-23 | `app/builders/slide_builder.py` SYNC (REI-3: Semaphore in FASE 5.1). Layout map `SlideType→idx` default BP §07.3 (0-7) override per-instance. Helper placeholder lookup by type (TITLE/BODY/PICTURE/SUBTITLE/OBJECT). Image insert try/except → fallback "[Immagine non disponibile]" (BP §07.1 line 2304-2312). `_is_local_path` rigetta URL (invariante BP line 2148). Quiz con 4 opzioni A-D + marker ✓. BuildReport dataclass per diagnostics. Output `{out}/{id}_corso.pptx`. 22 test su template sintetico (default python-pptx, generato in-process). D29 (BP §07.1 non dà classe), D30 (path app/builders/ vs prompt builders/). 176/176 verdi, mypy/ruff verdi. **#R8 ancora aperto** (template umano per validare layout BP §07.3 reali). |
| 4.3 | Image Service + sanitize_svg | **S** | BP fornisce codice esatto, pattern async con semaforo | ✅ | 2026-05-23 | `app/services/image_service.py` BP §07.0 verbatim: `sanitize_svg()` INLINE (FIX-2, 4 regex DOTALL su script/foreignObject/remote-xlink/event-handler); `_download_one_image()` async con Semaphore(5), cache DB query/usage_count, Pillow `load()` strict, guard 5MB; `_render_diagram_sync()` cairosvg 1200×800 (sanitize PRIMA del rendering); `prefetch_images()` con shared httpx client + asyncio.gather. 22 test (7 sanitize_svg deterministici + 5 download + 4 render + 5 prefetch + 1 meta-test FIX-2). D31 (bug BP `'web_tasks' in dir()`), D32 (dict typing), D33 (assert query_url not None). `cairosvg`/`cairosvg.*` in mypy overrides. 198/198 verdi, mypy/ruff verdi. |
| 4.4 | PDF Builder (Jinja2 + WeasyPrint) | **S** | Template HTML/CSS + rendering, logica lineare | ✅ | 2026-05-23 | `app/builders/pdf_builder.py` + `app/templates/dispensa.html` BP §07.2 + OPT-3 v3.0: Jinja2 sostituisce `str.format()`. Template con loop modules/slides, condizionali normative_ref/speaker_notes/quiz, copertina+page counter, palette brand iniettata, autoescape ON. PdfBuilder SYNC; `render_html()` esposto per test deterministici. Lazy import weasyprint (D34: GTK runtime Windows). 24 test (4 group puri + 2 constructor + 12 render_html Jinja2 + 4 build mockato + 1 escaping + 1 meta-test OPT-3 AST D35). D36: path `app/builders/`. `weasyprint`/`weasyprint.*` in mypy overrides. Nuova risorsa **#R12** (GTK runtime locale per validare PDF binario reale; workaround stub `sys.modules['weasyprint']=MagicMock()` nei test). 222/222 verdi, mypy/ruff verdi. |
| 4.5 | ProductionBuilder (orchestratore) | **S** | Composizione di moduli già pronti, pattern semplice | ✅ | 2026-05-23 | `app/builders/production_builder.py` + `app/builders/pptx_validator.py` BP §07.1 verbatim, karpathy applicato (zero recovery extra). `check_memory_before_build` (psutil, 60% threshold), `check_disk_before_build` (1GB min). ProductionBuilder.build async orchestratore: memory→disk→PPTX(to_thread)→validate→PDF(to_thread)→`_cleanup_tmp`(>1h)→`_build_report`. Progress 87/92/95 BP literal. PptxValidator carica .pptx + count check. 15 test (4 guard, 3 validator REAL pptx, 1 E2E 20 slide mock + 2 propagation/fail-fast, 2 cleanup, 3 meta REI-3/BP constants). D37 (PptxValidator minimum), D38 (path), D39 (top-level GenerationReport). `psutil`/`psutil.*` in mypy overrides. 237/237 verdi project-wide, mypy/ruff verdi. |
| 4.6 | Audio Service (edge-tts) | **S** | API async semplice, pattern identico a image_service | ✅ | 2026-05-23 | `app/services/audio_service.py` GAP-3 + OPT-1 edge-tts (NO OpenAI). Schema audio_tracks verificato da 001_initial.sql (D42, MCP postgres non esposto in sessione). `generate_narrations()`: Semaphore(3) + tenacity 3-retry exp + wait_for 30s, MP3 in `output/audio/{course_id}/slide_NNNN.mp3`, durata via mutagen, INSERT audio_tracks + UPDATE courses.audio_manifest_path, manifest JSON con `{course_id,total_tracks,tracks:[...]}`. Fallback per-slide (BP §07.1 invariante). ProductionBuilder esteso con tappa audio condizionata su `course["outputs"]` (progress 96, import lazy). D40 (body fallback senza LLM rephrase: karpathy #2), D41 (path), D42 (schema da migration). Nuova risorsa **#R13** (Edge TTS endpoint). 9 test (6 happy path + 3 fallback con cleanup file parziale + timeout). 246/246 verdi, mypy/ruff verdi project-wide. |
| 4.7 | Test E2E Build sintetico | **S** | Script di test, non logica nuova | ✅ | 2026-05-24 | `scripts/synth_build_test.py` CLI E2E: 30 slide mock + 3 moduli + outputs=["pptx","pdf","audio"]. Self-supplies template sintetico + `_FakePool` stub (no #R2). Guard `_check_weasyprint_available()` exit 2 con istruzioni Docker se GTK mancante. Verifica PPTX 30 slide + PDF esiste + audio MP3 + manifest JSON. **Eseguito con SUCCESSO IN DOCKER** (eduvault-backend, 2026-05-24): 30 slide + PDF + 30 MP3 it-IT-DiegoNeural REALI da edge-tts + manifest in ~10s. D43 (build/build_course), D44 (python-multipart pre-esistente), D45 (Docker rebuild). 246 pytest verdi + 1 synth E2E green. **FASE 4 INTERA COMPLETATA: 7/7 sotto-fasi ✅.** |

**Stima costo FASE 4:** ~$6-10 (6 Sonnet + 1 Opus)

---

### FASE 5 — Orchestrazione Backend + WebSocket + REST (Sprint 5A)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 5.1 | `generation_service.py` | **O** | ⚠️ Cuore dell'orchestrazione: semaforo, timeout globale, pipeline inner con costruzione initial_state, fingerprint, telemetria. Molte parti mobili interconnesse. | ✅ | 2026-05-24 | `app/services/generation_service.py` BP §09 verbatim. `_job_semaphore=Semaphore(1)` QUI (REI-3 + FIX-7). `PIPELINE_TIMEOUT_SECONDS = settings.pipeline_timeout` (OPT-2 D47). `send_ws_progress` (UPDATE progress_percent+current_step). `build_normative_fingerprint` (refs dedup + chunk_count + ISO timestamp). `run_pipeline` wrapper async with semaforo + asyncio.wait_for + 3 except (TimeoutError→'failed', CancelledError→'cancelled' + re-raise D51, Exception→'failed' truncate 500ch). `_run_pipeline_inner`: fetchrow course+brand → initial_state 8 campi BP §05.2 → status='research' → async with create_pipeline (D49) → ainvoke con cast (D50) → slide_contents_json + fingerprint + chunk_ids PRIMA del build → status='building' → prefetch_images → ProductionBuilder.build(db=pool D52) → status='completed' + INSERT audit_log pipeline_metrics. `recover_interrupted_jobs` BP §09.2: UPDATE status='failed' WHERE status IN (research/content/building). 17 test (3 invarianti REI/FIX/OPT + 3 fingerprint puri + 1 ws + 1 happy path + 3 failure paths + 1 ordering fingerprint-before-build + 2 recover + 3 meta). D47-D52 segnalate. 270/270 verdi, mypy/ruff verdi project-wide, codegraph reindex. |
| 5.2 | Endpoint `/api/courses` | **S** | CRUD + download, pattern standard FastAPI | ✅ | 2026-05-24 | `app/api/routes/courses.py` BP §10: POST (rate 5/min BP §10.4, pacing estimate, queue_position via COUNT generation_jobs, INSERT course+job, fire-and-forget `asyncio.create_task(run_pipeline)`), GET paginato ownership-aware (admin tutti, operator solo `created_by=me`) + filtro status, GET/{id} con fingerprint JSON parse + 404/403, POST/{id}/certify (reviewer/admin → `certify_course`), GET/{id}/download/{pptx,pdf,zip,audio} (StreamingResponse ZIP in-memory, "audio" zippa cartella MP3 + manifest), DELETE soft-delete archived. `certification_service.py` MINIMUM (D53: FASE 7.1 lo sostituirà BP §06.2 verbatim). 19 test (unauth/queue position/invalid outputs/ownership SQL admin vs operator/status filter/404-403-200/certify role+ValueError→400/download tutti i 4 fmt + missing path 404 + invalid 400/delete owner vs non-owner). Router registrato in main.py. 289/289 verdi, mypy/ruff verdi project-wide, codegraph reindex. |
| 5.3 | WebSocket progress autenticato | **S** | BP §08.8 fornisce codice esatto, pattern asincrono | ✅ | 2026-05-24 | `app/api/websocket.py` BP §08.8 verbatim + 3 fix REI/security: WS `/ws/jobs/{job_id}?token=...` con JWT decode → close 4001 invalid/refresh (D56), JOIN courses-jobs per ownership → close 4004/4003 (admin/reviewer = qualunque job, operator = solo `created_by=me`), loop `send_json + sleep(1)` fino a TERMINAL_STATES `{completed,failed,cancelled}` (D55 estende BP per coerenza con FASE 5.1 D51). `get_job_progress(job_id)` riusabile per polling fallback con UUID conversion + try/except → not_found su UUID malformato (D54 fix bug BP). Router registrato in main.py. 14 test (3 get_job_progress puri + 3 auth failures incl. refresh-token reject + 3 ownership 4004/4003 + 4 happy paths admin/operator/multi-frame/failed + 1 meta). 303/303 verdi, mypy/ruff verdi project-wide, codegraph reindex. |
| 5.4 | Endpoint `/api/admin` | **S** | Query aggregate semplici | ✅ | 2026-05-24 | `app/api/routes/admin.py` BP §10: 4 endpoint admin-only (D57). GET /api/admin/metrics aggrega `audit_log WHERE action='pipeline_metrics'` su `?days=N` (1-365, default 7) → total_runs/avg_elapsed_seconds/avg_slides/total_images_resolved/period_days, gestisce sia JSONB dict che str (asyncpg codec variance). GET /api/dashboard/stats → COUNT su courses/regulations/approved_courses con guard NULL→0. GET /api/brand-presets → SELECT ORDER BY is_default DESC, name + parsing palette JSON-or-dict. GET /api/catalog → ritorna COURSE_CATALOG (6 tipi BP §13). Router registrato in main.py. 12 test (per endpoint: admin OK + operator 403; metrics: empty + mixed payload + invalid days 422; dashboard: null counts; brand-presets: default first + palette dict/str; catalog: 6 keys; unauth surface su tutti). 315/315 verdi, mypy/ruff verdi project-wide, codegraph reindex. |
| 5.5 | Smoke test E2E backend + Audit checklist | **S** | Verifica sequenziale, no logica nuova | 🟡 | 2026-05-24 | **E2E PARZIALE eseguito live**. Steps 1-6 verdi: down -v → build → up → migrations (10 tabelle) → setup_roles con sed sub password → seed (admin+brand) → restart backend → /health OK → login admin → /me. Step 7-9 (ingest dm388 + POST /api/courses + WS): **BLOCKED-UPSTREAM** da #R1 (PDF reale assente, solo synthetic) + #R3 (ANTHROPIC_API_KEY restituisce 401 invalid x-api-key live confermato) + #R4 (VOYAGE non testato ma stessa categoria). Fallback surrogate (deciso con utente): synth_build_test re-eseguito in Docker (30 slide + PDF 25KB + 30 MP3 reali edge-tts), poi INSERT manuale di 1 corso completed in DB → tutti i 4 download REST testati REAL (PPTX 89496B, PDF 25465B, ZIP 69183B, audio ZIP 1.4MB con 30 MP3+manifest) + tutti i 4 admin endpoint REST testati REAL (metrics→0 runs, dashboard→1+1+0, brand-presets→default, catalog→6 keys). D58 (ingest non transazionale). **Audit checklist FASE 5 completo (12 item)**: 11 ✅ + 1 FIX item 8 (polling fallback doc in `docs/POLLING_FALLBACK.md`). Item 11 audit_log append-only verificato LIVE in Docker. **Per chiusura completa servono #R1+#R3+#R4 risolti** (utente deve fornire PDF reale + API keys valide). |

**Stima costo FASE 5:** ~$6-9 (4 Sonnet + 1 Opus)

---

### FASE 6 — Frontend shadcn-admin + Branding (Sprint 5B)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 6.1 | Clone template shadcn-admin | **S** | Comandi git, zero ragionamento | ✅ | 2026-05-24 | shadcn-admin v2.2.1 @ commit `a6352e7` clonato in `frontend/`; `.git` rimosso; `INTEGRATION_NOTES.md` con hash + data + regola adattamento + 3 fatti rilevanti (pnpm-lock, Clerk preinstallato D60, Tailwind v4). Zero `npm install`. |
| 6.2 | Analisi struttura + inventario | **S** | Esplorazione file + report Markdown | ✅ | 2026-05-24 | Inventario read-only completo aggiunto a `INTEGRATION_NOTES.md`: 30 ui primitives, 8 features riutilizzabili (dashboard, tasks→corsi, users, auth/sign-in, errors), 7 da rimuovere (clerk/**, apps, chats, sign-up, otp), 5 nuove da creare. Sistema temi documentato (OKLCH in `src/styles/theme.css`, Tailwind v4 senza `tailwind.config.ts`). |
| 6.3 | Iniezione brand C.F.P. Montessori | **S** | Sostituzione variabili CSS e config Tailwind, meccanico | ✅ | 2026-05-24 | Palette `#C82E6E`+`#769E2E` → OKLCH precisione Python (sRGB→LMS→OKLab); brand tokens additivi in `theme.css` (`--brand-primary/secondary/hover/foreground` + esposti in `@theme inline` come utility `bg-brand-*`); `--primary`+`--ring`+`--chart-1`+`--chart-2` mappati a brand; dark mode con tinte schiarite (`#E85A91`/`#A3D055`); `--accent`/`--secondary` NON sovrascritti (D64 — neutri template enterprise); 10 varianti logo via Pillow in `frontend/public/brand/` (D66, #R7 parzialmente mitigato); sidebar header `AppTitle` con logo-mark + "Cfp EduVault" + "C.F.P. Montessori" (D65 footer pendente per 6.5); `index.html` title+meta+favicon+theme-color rebrandizzati. **Verificato LIVE via Chrome DevTools MCP**: screenshot light (dashboard rosa CTA, grafico rosa, sidebar logo) + dark (rosa schiarito leggibile su nero, contrasto AA) green, 0 errori console. |
| 6.4 | Tipi TypeScript da OpenAPI | **S** | Comando npx + verifica | ✅ | 2026-05-24 | Backend `docker compose up -d` → `curl http://localhost:8000/openapi.json -o frontend/src/lib/openapi.json` (3.1.0, 16 paths, 21 schemas); `npx openapi-typescript@7.13.0 -o frontend/src/lib/types.gen.ts` → 1171 LOC, 18 schemas. Verifica 5/5 target presenti (D67 naming: `User`→`UserMe`, `Regulation`→`RegulationSummary`, `BrandPreset`→`BrandPresetSummary`). `npm install` saltato (D61: avrebbe rotto `node_modules` pnpm). Aggiunto Bloccante #2 Docker a `docs/HANDOFF_PHASE6.md`. |
| 6.5 | API Client + WebSocket Client | **S** | Client fetch tipizzato, pattern ripetitivo per N endpoint | ✅ | 2026-05-24 | `frontend/src/lib/api.ts` (330 LOC) copre 17/17 endpoint BP §10 (auth/courses/regulations/admin) + `downloadCourse` con 4 formati (pptx/pdf/zip/audio); JWT localStorage `nexus.accessToken`+`refreshToken` + single-flight refresh on 401 + `auth:logout` event; `ApiError` + `tokenStorage`. `frontend/src/lib/ws.ts` (290 LOC): `connectToJob(jobId, opts)` con WS path reale `/ws/jobs/{job_id}?token=`, polling fallback 30s via `getCourse(courseId)`, retry 2× backoff, close codes 4001/4003/4004 = unrecoverable, mapping `CourseDetail.status` → `JobProgress`. `.env.example` `VITE_API_URL=http://localhost:8000`. 9 discrepanze REI-16 D68-D76. tsc verde. |
| 6.6 | Pagina Login | **S** | Adattamento minimo di pagina esistente nel template | ✅ | 2026-05-24 | `features/auth/sign-in/index.tsx` + `user-auth-form.tsx` riscritti per `api.login()`; copy italiano; rimossi GitHub/Facebook (REI-4) + forgot-password (BP §08 v1.0); `AuthLayout` con logo C.F.P. centrato + "Cfp EduVault" + tagline "Generazione corsi normativi"; `<title>` setter via useEffect; PasswordInput template + autoComplete + autoFocus + aria-busy. Test `user-auth-form.test.tsx` riscritto: 5 test contro nuovo contratto (mock api.login + toast IT). 5 discrepanze REI-16 D77-D81. Verificato LIVE Chrome DevTools light + dark (logo trasparente leggibile su nero). |
| 6.7 | Dashboard + Lista Corsi | **O** | ⚠️ Pagina complessa | ✅ | 2026-05-24 | 12 file: `features/dashboard/` con `index.tsx` + 6 components (stats-cards/courses-table/courses-columns/courses-row-actions/delete-course-dialog/courses-primary-buttons/course-status-badge) + `data/courses-meta.ts`. 4 KPI cards (Corsi totali/In generazione/Normative/L2) + tabella TanStack con 7 colonne + filter URL-sync + status badge a 5 varianti (amber/blue/brand-green/red/grey) con pulse animation su 'generating'. react-query refetchInterval 30s. Role decoded inline da JWT. **CORS fix** `.env` 3000→5173. 8 discrepanze REI-16 D82-D89. Verificato LIVE con corso reale generato. |
| 6.8 | Wizard 6-step Creazione Corso | **O** | ⚠️ La pagina frontend più complessa | ✅ | 2026-05-24 | 10 file: `features/courses-wizard/` con `index.tsx` (230 LOC shell) + 6 step components + `schema.ts` (zod allineato CourseRequest Pydantic) + `step-indicator.tsx` (custom progress, 50 LOC). Form react-hook-form + zodResolver + step-by-step `form.trigger(fields)`. Step 1→3 cross-step intelligence (catalog min_hours pre-fill). Step 4 brand swatches reali. Step 6 stima slide = `duration_hours × 120` identica al backend PacingEngine. **POST `/api/courses` verificato LIVE**: 200 OK, `estimated_slides=480` (4h × 120). 7/7 campi CourseRequest coperti. 7 discrepanze REI-16 D90-D96. |
| 6.9 | Progress + Dettaglio + Normative + Admin | **O** | ⚠️ 4 pagine in un prompt | ✅ | 2026-05-24 | 12 file: aggiunto `@radix-ui/react-progress` + `ui/progress.tsx`. **Progress Monitor** (`course-progress/`): phase machine 5 fasi derivata da `status`+`current_step`, WS+polling+retry+redirect, error retry button. **Course Detail**: info+status badge, 3 download cards (PPTX/PDF/Audio), fingerprint Collapsible (provenance citation BP §00), Certifica (admin+reviewer), Archivia. **Regulations**: list+filter+badge VIGENTE/ABROGATA, UploadDialog drag-drop HTML5 nativo (zero dep), ChunksSheet paginated. **Admin**: 6 metric cards + brand preset grid + **stub onesto utenti** (`/api/users` non esiste backend REI-5, snippet psql workaround). 4 route create. 10 discrepanze REI-16 D97-D106. Verificato LIVE tutte 4 pagine. |
| 6.10 | Navigazione, Routing + Build | **S** | Configurazione router, sidebar, guard | ✅ | 2026-05-24 | Reorganization route: `_authenticated/index.tsx` → redirect `/dashboard`, `dashboard.tsx` route ufficiale. `(auth)/login.tsx` sostituisce `sign-in.tsx` + redirect-if-auth. Eliminati 5 file route orfani auth (sign-up/forgot-password/otp/sign-in-2/sign-in). `lib/auth.ts` con `isAuthenticated()`+`getRole()`+`decodeAccessToken()`. **Auth guard** su `_authenticated/route.tsx` `beforeLoad`: senza JWT valido → redirect `/login?redirect=<href>`. Sidebar role-aware (Conoscenza+Amministrazione admin-only). NavUser footer email/role real-time da `/api/users/me` + JWT. SignOutDialog usa `api.logout()`+naviga `/login`. Global `auth:logout` listener in `main.tsx`. tsconfig.app.json exclude 4 path orfani (REI-1 pragmatic). **pnpm/vite build production**: ✅ verde 3.37s, 56 chunks, 0 errori, 0 warning. **Smoke E2E LIVE Login→Dashboard→Wizard→Progress eseguito** (Chrome DevTools MCP). 21 discrepanze REI-16 D107-D127. **Audit 20-item checklist eseguito**: 18 ✅ + 2 ⚠️ parziali (item 15 gestione utenti = stub onesto perché backend non espone endpoint; item 20 Download blocked-upstream da #R3 Anthropic + #R11 LangGraph checkpoint setup — frontend pronto e verificato fino a Progress page). |

**Stima costo FASE 6:** ~$12-18 (7 Sonnet + 3 Opus)

---

### FASE 7 — Certification, Audit, Deploy (Sprint 6)

| # | Sotto-fase | Modello | Motivazione | Stato | Data | Note |
|---|---|---|---|---|---|---|
| 7.1 | Certification Service (StylePatternExtractor) | **S** | BP §06.2 fornisce codice esatto, estrattore deterministico | ⬜ | | |
| 7.2 | Audit log + Cleanup | **S** | INSERT in endpoint esistenti, job schedulato semplice | ⬜ | | |
| 7.3 | Testing E2E completo | **O** | ⚠️ Test che attraversa l'intero stack: ingestion → pipeline → build → certificazione → recovery. Richiede comprensione globale del sistema per scrivere asserzioni corrette. | ⬜ | | |
| 7.4 | Backup & Ops docs | **S** | Script bash + Markdown, pattern standard | ⬜ | | |
| 7.5 | Deploy su VPS | **S** | nginx.conf + compose.prod + script install, pattern standard | ⬜ | | |

**Stima costo FASE 7:** ~$5-8 (4 Sonnet + 1 Opus)

---

## RIEPILOGO STRATEGIA MODELLO

### Distribuzione per fase

| Fase | Sotto-fasi totali | Sonnet | Opus | % Opus | Stima costo |
|---|---|---|---|---|---|
| Step A | 2 | 2 | 0 | 0% | $1-2 |
| Step B | 3 | 3 | 0 | 0% | $1-2 |
| Step C | 2 | 2 | 0 | 0% | $1 |
| FASE 0 | 6 | 6 | 0 | 0% | $2-4 |
| FASE 1 | 5 | 5 | 0 | 0% | $3-5 |
| FASE 2 | 6 | 4 | **2** | 33% | $8-12 |
| FASE 3 | 5 | 3 | **2** | 40% | $10-15 |
| FASE 4 | 7 | 6 | **1** | 14% | $6-10 |
| FASE 5 | 5 | 4 | **1** | 20% | $6-9 |
| FASE 6 | 10 | 7 | **3** | 30% | $12-18 **(10/10 chiusi ✅ — FASE COMPLETATA)** |
| FASE 7 | 5 | 4 | **1** | 20% | $5-8 |
| **TOTALE** | **56** | **46** | **10** | **18%** | **$55-86** |

### Le 10 sotto-fasi che giustificano Opus

| # | Fase.Sotto-fase | Perché Opus |
|---|---|---|
| 1 | **2.2** Chunking Ibrido | Algoritmo custom con regex normative italiane, coverage check, fallback. Il cuore IP del prodotto. |
| 2 | **2.5** KnowledgeRepository + RAG | Query vettoriale + JOIN regionale + threshold. Errori = corsi con contenuto sbagliato. |
| 3 | **3.3** Research Agent | Orchestrazione RAG end-to-end: query semantica, distribuzione chunk, ribilanciamento. |
| 4 | **3.4** Content Agent | La sotto-fase PIÙ complessa: LLM calls, JSON parsing, circuit breaker, prompt Discente/Formatore. |
| 5 | **4.2** SlideBuilder | Manipolazione python-pptx pixel-level con fallback e multi-layout. |
| 6 | **5.1** generation_service.py | Orchestrazione pipeline con semaforo, timeout, fingerprint, telemetria. Molte parti mobili. |
| 7 | **6.7** Dashboard + Lista Corsi | UI complessa: DataTable, badge stato, filtri, paginazione, azioni. |
| 8 | **6.8** Wizard 6-step | Form multi-step condizionale con validazione e dati da 3 endpoint. |
| 9 | **6.9** 4 pagine (Progress, Dettaglio, Normative, Admin) | Volume alto, WebSocket real-time, upload, coerenza visiva. |
| 10 | **7.3** Testing E2E completo | Test full-stack che richiede comprensione globale dell'architettura. |

### Regola pratica per decidere in tempo reale

```
SE il prompt dice "come BP §XX" e la BP fornisce codice esatto
  → SONNET (traduzione spec → codice)

SE il prompt dice "implementa l'algoritmo/la logica per..."
  E coinvolge >3 file interconnessi
  O richiede gestione di edge case non documentate
  O produce output che impatta la qualità del prodotto finale
  → OPUS (ragionamento architetturale)

SE hai dubbi
  → Inizia con SONNET. Se dopo 2 tentativi il codice ha bug strutturali
  → Passa a OPUS per quella sotto-fase
```

---

## TIMELINE STIMATA

| Settimana | Fasi | Ore umane stimate |
|---|---|---|
| 1 | Step A + B + C | 4-6h |
| 2 | FASE 0 + FASE 1 | 8-12h |
| 3 | FASE 2 | 10-14h |
| 4 | FASE 3 | 12-16h |
| 5 | FASE 4 | 10-14h (+ 4-6h lavoro umano template PPTX) |
| 6 | FASE 5 | 8-12h |
| 7-8 | FASE 6 | 14-20h |
| 9 | FASE 7 + QA finale | 10-14h |
| **TOTALE** | | **76-114h** (~6-9 settimane) |

---

## v2 REFACTOR — Universale RAG+Graph, Coerenza, Image Library, Chat Studio

Piano: `~/.claude/plans/vast-hopping-sketch.md` (approvato analista + vincolo VAA).
Decisioni D1..D10 risolte. Ordine: D8 → D2+D1 → D3 → D9 → D4/D5 → D7 → D6 → D10.

| Fase | Descrizione | Stato | Data | Note |
|------|-------------|-------|------|------|
| F0 | Setup: migrazione 005 (7 tabelle), feature flags, deps, telemetria | ✅ | 2026-05-28 | Migrazione in prod 7/7 tabelle + 3 col `courses` + 22 idx, idempotente. Flag tutti False → v1 invariata. mypy/ruff verdi. Scoperto #R18 (`nexus_app` assente → GRANT condizionale). |
| F1 | Catalogo cliente in DB (D8) + 3 normative TIER 1 + bug citation_label + UI normativa↔corso + admin UI catalog-review + approve | ✅ | 2026-05-31 | 44 corsi attivi + 195 moduli + 102 link `regulation_course_type_links` in DB prod. 3 normative TIER 1 ingerite (Reg CE 852/2004 HACCP 147ch, DM 2/9/2021 antincendio 58ch chiude #R16, Reg CE 1272/2008 CLP 672ch). citation_label bug fixato (2879/2879 backfilled + ingestion patch a regime). Endpoint `GET /regulations/{slug}/linked-courses` + UI ChunksSheet "Corsi che usano questa normativa (N)" verificata E2E in prod via Chrome DevTools. **F1.D.2 chiuso 2026-05-31**: backend `app/services/catalog_service.py` + 6 endpoint admin REST (`/api/admin/catalog{/summary,,/<slug>,/<slug>/approve,/<slug>/unapprove,/bulk-approve}`) + frontend `/admin/catalog` con tabella shadcn (summary 3-card + breakdown per target chip filter + filtri search/target/approved-only + bulk approve checkbox + dialog dettaglio moduli ordinati + bottoni Approva/Revoca per riga). CTA dalla pagina Admin. Flag `v2_catalog_from_db=false` di default (safety-net D10): config-driven path attivo finche' admin non approva tutti i 44 entries + flip flag su Railway. mypy clean, tsc clean, vite build verde 5.57s. Commit 86eeca8 push deploy SUCCESS. D-183 in VERIFICATION_DEBT (scope MVP ridotto: scraping script + research_agent branch posticipati a post-flip flag). |
| F2 | Rerank 2-stadi (D2) + Edge Graph deterministico (D1) | 🔴 REGRESS | 2026-05-29 | **Step 1-8 implementati MA REVIEW 17 ANALISTA = BOCCIATA**. Cross-corso contamination ~50-90% (PRE M3, GEN M1+M2, ANT M0). Le 5 cure universali in entrata: D3 scheletro narrativo + filtro title-alignment + KG sibling restrizione + D9 vincolante + corpus antincendio. Flag spenti su Railway prod, 3 V2 archiviati, F2.11 sospesa. **STORICO step 1-8**: Step 1-4: Cohere rerank + retrieval_v2.py (470 righe) mypy/ruff verdi, smoke test 4/4 PASS top_score 0.97+, research_agent branch behind flag con smoke 3 moduli semanticamente perfetti. Step 5-8: graph_service.py (520 righe) mypy/ruff verdi, smoke 4/4 PASS post-refactor (`_ChunkResolver` cache + `executemany` batch — la versione N+1 droppava il TCP proxy Railway sul D.Lgs 81/08). Ingestion hook POST-embed dietro flag. Backfill 7 normative VIGENTE in 78s: **21451 edge** (18308 gerarchico_sibling + 2105 parent + 1003 cita + 26 attua + 9 modifica, tutti `source=deterministic`). 1-hop traversal: smoke prod dlgs_81_08 "Concetti generali" → 30 rerank + 148 kg_1hop = 178 totali, top rerank Art.15 (0.970) → top kg_1hop Art.6 (0.652, decay 0.7 × edge.weight 1.0 × 0.96), edges_followed=267. VAA-b `source` tracciata in tutti gli step (`rerank_cohere`/`kg_1hop`/`rrf_fallback`); VAA-d sensore D9 attivo; VAA-e flag `v2_rerank_enabled` e `v2_kg_traversal_enabled` default OFF. **Resta F2.9** (A/B prod: richiede ingestione Antincendio L1 + flag-on temporaneo Railway + generazione 2 corsi paralleli ~30 min, decisione utente sulla modalità). |
| F3 | Scheletro narrativo validato (D3) + F3.AI micro-actions LLM | 🟡 | 2026-05-31 | Backend D3 chiuso end-to-end (skeleton_service + materialize per-subtopic + pipeline interrupt + API GET/PUT/POST + UI manuale). **F3.AI nuova 2026-05-31**: rename "Approva scheletro"→"Approva struttura" + 3 pulsanti AI per voce (Riformula/Operativo/Alternative con DiffAcceptDialog/AlternativesDialog) + free-text "Chiedi all'AI" per modulo (ModuleAiPrompt preview dialog). Backend pure-proposal `app/services/skeleton_ai_edit_service.py` + 2 endpoint courses.py rate-limited. Commit eba5415, deploy SUCCESS. Smoke API prod gate OK (400/409/404). **Resta**: E2E UI completo su corso reale skeleton_pending (oggi 0 in prod, primo corso reale validerà UI cliente). D-182 REI-16 in VERIFICATION_DEBT. |
| F4 | Badge UI slide problematiche (D9) + F4b rigenera slide H8 voce-aware | ✅ | 2026-05-31 | F4: `slide_quality_service.py` (11 issue types deterministici) + migration 010 + endpoint `GET /quality-issues` + frontend QualityBadge/Panel/Summary integrati in CourseStudio (3-col grid responsive 180/1fr/320 lg, 220/1fr/360 xl). F4b: `regenerate_single_slide_h8()` voce-aware (re-invoca generate_module_structured con voice skeleton owner + chunks materializzati B2+B3+B4) + endpoint `POST /slides/{idx}/regenerate?use_h8=true`. E2E Playwright LIVE prod course 691405b1: 208 issues catured, regenerate 35s con persistenza DB dirty=true, badge sidebar rosso→blu, summary 6→5 errors. |
| F5 | Image library (D4) + Diagram catalog + ISO 7010 (D5) | 🟡 | 2026-05-31 | **POST-MVP step 1 chiuso**: backend `app/services/embeddings.py` (voyage-multimodal-3 image/text), `image_library_service.py` (k-NN cosine + GIN fallback, threshold 0.30), `diagram_router.py` (7 SVG heuristic confidence ≥0.5 + log structured), migration 011 (idx supplementare per UI admin). Integration cascade tier-0 in `image_service._resolve_query_urls._one` PRIMA di Pexels. 2 endpoint REST: `/courses/{id}/image/library/search` (rate 60/min) + `/api/admin/images/library` (paginato + filtri). Frontend `image-picker.tsx` riscritto con shadcn Tabs Library/Web (default Library) + LibraryCard hover license chip + score badge brand-primary + Tooltip attribution. **Test 20/20 PASS** (9 test_embeddings mock Voyage + 11 test_diagram_router heuristic). tsc -b strict + vite build CLEAN 7.11s. Discrepanze D-184/D-185/D-186 in DEBT (cascade ordering invertito Pexels→Library; seed scope ridotto NO scraping auto; 8 SVG nuovi + ISO7010 posticipati a F5.next). **Resta F5.next**: scripts/seed_image_library con manifest reali (~50 ISO7010 PNG + ~200 Wikimedia/Openverse curati), 8 SVG nuovi (timeline/swimlane/fishbone/venn/decision_tree/cycle_pdca/gantt_mini), icon-library ISO 7010. |
| F6 | Chat LLM Course Studio (D7) | ⬜ | — | F1 per-slide attivo, F2 scaffolding dietro flag |
| F7 | Azure Speech SDK + pre-render (D6) | ⬜ | — | SSML completo, edge-tts fallback |
| F8 | Cleanup A/B per famiglia (D10) | ⬜ | — | Rimuove drop-list/prose/catalog_config solo dopo prova A/B |
| F9 | Regulation→Courses Discovery (richiesta cliente 2026-05-31) | ✅ | 2026-05-31 | Backend `regulation_discovery_service.py` (lookup inverso COURSE_CATALOG + count chunks per coverage) + endpoint `GET /regulations/{slug}/compatible-courses` (auth get_current_user, ritorna courses con overall_coverage generabile/corpus_thin/no_coverage). Frontend collapsible "Corsi generabili da questa normativa" in ChunksSheet con badge colorati + bottone Genera (navigate /courses/new?course_type=slug). E2E Playwright LIVE prod: D.Lgs 81/08 → 6 corsi compatibili (Formazione Preposti 8h ✓ generabile, Lavoratori Generale 4h ✓ generabile, Specifica Basso 4h ✓ generabile, Antincendio L1 ⚠ corpus_thin, Primo Soccorso A ⚠ corpus_thin, Primo Soccorso B/C ⚠ corpus_thin) tutti con bottone Genera. Commit 0ae3837. |

---

## NOTE OPERATIVE

1. **Configura il modello in Claude Code prima di ogni sotto-fase:**
   ```bash
   # Per sotto-fasi Sonnet:
   claude config set model claude-sonnet-4-6
   
   # Per sotto-fasi Opus:
   claude config set model claude-opus-4-6
   ```

2. **Monitora i costi:** dopo ogni fase, controlla il consumo su https://console.anthropic.com → Usage. Se una fase Sonnet costa più del previsto, non passare a Opus — il problema è probabilmente nel prompt, non nel modello.

3. **Prompt caching:** Claude Code usa il caching automatico. Sessioni lunghe con contesto stabile (stessa Blueprint in memoria) beneficiano del 90% di sconto sugli input token cachati. Motivo in più per fare **una sessione per fase** come da piano.

4. **Il modello indicato è una raccomandazione, non un vincolo.** Se Sonnet produce codice corretto al primo tentativo su una sotto-fase marcata Opus, tanto meglio — hai risparmiato. Se Sonnet fallisce su una sotto-fase semplice dopo 2 tentativi, passa a Opus senza esitare.

---

*Documento da aggiornare ad ogni fase completata. Stampare e tenere accanto al monitor durante lo sviluppo.*
